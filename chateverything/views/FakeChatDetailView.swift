import SwiftUI
import AVFoundation
import Speech
import LLM

struct ChatDetailView: View {
    var body: some View {
        VStack {
            Text("Fake Chat Detail View")
        }
        .padding()
    }
}

